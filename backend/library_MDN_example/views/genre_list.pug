doctype html
html
  head
    title Genre List
  body
    h1 Genre List
    ul
      each genre in genre_list
        li
          a(href="/genres/#{genre._id}") #{genre.name}
        ul
      each author in author_list
        li 
         a(href="/authors/#{author._id}") #{author.fname}
        ul